# ── Brewfile (macOS-only remnants) ─────────────────────────────────────────
# Most packages have moved to home-manager (home/common.nix).
# Casks are managed by nix-darwin (modules/darwin.nix).
#
# This file is kept as a fallback for:
#   1. Packages not yet available in nixpkgs
#   2. System libraries that other brew formulae depend on
#   3. Manual bootstrapping before nix-darwin is set up
#
# Once nix-darwin is active, run: darwin-rebuild switch --flake ~/dotfiles#work-mbp

# ── Formulae not (yet) in nixpkgs ──────────────────────────────────────────
brew "opencode"
brew "gemini-cli"

# ── System libraries (may be needed by other brew formulae) ────────────────
# These are typically pulled in as dependencies on NixOS.
# Keep them here only if something on macOS links against them directly.
# brew "gnutls"
# brew "icu4c"
# brew "libidn"
# brew "unixodbc"
# brew "zlib"
